const bookmarkList = [];

function Book(title, author, pages, year, haveRead) {
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.year = year;
    this.haveRead = haveRead;
}

function addBooktoLibrary() {
     
}